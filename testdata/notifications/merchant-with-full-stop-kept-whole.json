{
  "sender": "Google Pay",
  "packageName": "com.google.android.apps.nbu.paisa.user",
  "provenance": "synthetic",
  "note": "Synthetic. It pinned a KNOWN DEFECT and now pins the fix (SPEC Q17). `notification-upi-paid` ended the merchant group on `(?:\\.|\\busing\\b|\\bfrom\\b|$)`, so the FIRST full stop inside a merchant name terminated it and `Dr. Lal PathLabs` was captured as `Dr`. Real merchant names contain full stops -- `Dr.`, `M/s.`, `St.` -- and the truncation was short enough to look like a plausible name rather than an obvious error. A full stop now only ends the merchant at the end of the line (`\\s*\\.?\\s*$`); mid-line the merchant runs until `using` or `from`, which is what every real payload in this corpus uses. The expectation below is the corrected capture.",
  "expected": {
    "ruleId": "notification-upi-paid",
    "amountMinor": 72500,
    "currency": "INR",
    "direction": "DEBIT",
    "merchantRaw": "Dr. Lal PathLabs",
    "instrumentHint": "UPI"
  }
}
